import React from "react";
import styles from "./list-empty.module.css"
const ListEmpty = () => {
    return (  <div className={styles['list-empty']}>There is no user</div>);
}
 
export default ListEmpty;